package Connection;

public enum MessageType {
    REQUEST_USER_NAME,
    TEXT_MESSAGE,
    USER_NAME_ACCEPTED,
    SHOW_USER_NAME,
    NAME_USED,
    USER_ADDED,
    DISABLE_USER,
    REMOVED_USER;
}